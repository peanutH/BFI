<!doctype html>
<html lang="en" data-theme="dark">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Crimson+Pro:wght@400;500;600&family=DM+Sans:wght@400;500;600;700&family=Noto+Sans+SC:wght@400;500;600;700&family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
		<meta name="generator" content={Astro.generator} />
		<title>Big Five Personality Test | 大五人格测试</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		/* Dark theme (default) */
		--bg-primary: #0f1419;
		--bg-secondary: #1a2332;
		--bg-card: #1e2a3a;
		--accent-teal: #2dd4bf;
		--accent-teal-dim: rgba(45, 212, 191, 0.15);
		--text-primary: #f1f5f9;
		--text-secondary: #94a3b8;
		--text-muted: #64748b;
		--border-subtle: rgba(148, 163, 184, 0.1);
		--shadow-glow: 0 0 60px rgba(45, 212, 191, 0.08);
		--bg-gradient: 
			radial-gradient(ellipse 80% 50% at 50% -20%, rgba(45, 212, 191, 0.08) 0%, transparent 50%),
			radial-gradient(ellipse 60% 40% at 100% 100%, rgba(99, 102, 241, 0.05) 0%, transparent 40%),
			var(--bg-primary);
	}

	[data-theme="light"] {
		--bg-primary: #f8fafc;
		--bg-secondary: #e2e8f0;
		--bg-card: #ffffff;
		--accent-teal: #0d9488;
		--accent-teal-dim: rgba(13, 148, 136, 0.1);
		--text-primary: #0f172a;
		--text-secondary: #475569;
		--text-muted: #64748b;
		--border-subtle: rgba(71, 85, 105, 0.15);
		--shadow-glow: 0 4px 24px rgba(0, 0, 0, 0.08);
		--bg-gradient: 
			radial-gradient(ellipse 80% 50% at 50% -20%, rgba(13, 148, 136, 0.06) 0%, transparent 50%),
			radial-gradient(ellipse 60% 40% at 100% 100%, rgba(99, 102, 241, 0.04) 0%, transparent 40%),
			var(--bg-primary);
	}

	* {
		box-sizing: border-box;
	}

	html,
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		min-height: 100vh;
		font-family: 'DM Sans', 'Noto Sans SC', 'Noto Sans JP', -apple-system, BlinkMacSystemFont, sans-serif;
		background: var(--bg-primary);
		color: var(--text-primary);
		transition: background-color 0.3s ease, color 0.3s ease;
	}

	body {
		background: var(--bg-gradient);
	}

	/* Logic: Hide all localized elements by default */
	[class*="text-"] {
		display: none !important;
	}

	/* Show elements matching current data-lang */
	[data-lang="en"] .text-en,
	[data-lang="zh"] .text-zh,
	[data-lang="ja"] .text-ja,
	[data-lang="ko"] .text-ko,
	[data-lang="it"] .text-it,
	[data-lang="es"] .text-es,
	[data-lang="fr"] .text-fr {
		display: block !important;
	}

	/* Maintain inline/flex behavior for span elements */
	[data-lang="en"] span.text-en,
	[data-lang="zh"] span.text-zh,
	[data-lang="ja"] span.text-ja,
	[data-lang="ko"] span.text-ko,
	[data-lang="it"] span.text-it,
	[data-lang="es"] span.text-es,
	[data-lang="fr"] span.text-fr {
		display: inline !important;
	}

	/* Maintain flex behavior for elements that need it */
	[data-lang="en"] .nav-btn.text-en,
	[data-lang="zh"] .nav-btn.text-zh,
	[data-lang="it"] .nav-btn.text-it,
	[data-lang="es"] .nav-btn.text-es,
	[data-lang="fr"] .nav-btn.text-fr {
		display: flex !important;
	}

	[data-lang="zh"], [data-lang="ja"], [data-lang="ko"] {
		word-break: break-word;
	}
</style>
